define(['angular', 'math-engine', 'main-controller'], function (angular) {
    //var appName = 'streaming-knife';
    //angular.module(appName, []);

    var init = function () {
        // todo: bootstrap something if needed
    };

    return {
        init: init,
    };
});